<Window x:Class="PortKiller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="PortKiller" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResize"
        ShowInTaskbar="True"
        Visibility="Visible"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Boolean to Visibility Converter -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Modern Card Style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#2B2B2B"/>
            <Setter Property="BorderBrush" Value="#1FFFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="4" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#323232"/>
                    <Setter Property="BorderBrush" Value="#3FFFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Window Control Button Style -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1FFFFFFF"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#20FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Dark Sidebar Button Style -->
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#909090"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Margin="8,2">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#15FFFFFF"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Outline Button Style -->
        <Style x:Key="OutlineButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#30FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#10FFFFFF"/>
                    <Setter Property="BorderBrush" Value="#50FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern Search TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="#20FFFFFF"/>
                                <Setter Property="BorderBrush" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#20FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Invisible ScrollViewer Style (hides scrollbar but keeps scroll functionality) -->
        <Style x:Key="InvisibleScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <ScrollContentPresenter />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- System Tray Icon (created in code-behind) -->
    </Window.Resources>

    <!-- Main Container with Transparent Area for Blur -->
    <Border Background="Transparent" CornerRadius="16">
        <Border.Effect>
            <DropShadowEffect Color="#50000000" BlurRadius="40" ShadowDepth="10" Direction="270" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <!-- Background layer: Solid for right side, transparent for sidebar -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Left sidebar area: Transparent for blur -->
                <Border Grid.Column="0" Background="Transparent"/>
                <!-- Right content area: Solid background -->
                <Border Grid.Column="1" Background="#1A1A1A" CornerRadius="0,16,16,0"/>
            </Grid>
            
            <!-- Content on top of background -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Sidebar (Transparent Blur) -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- App Title (Draggable) -->
                    <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                        <StackPanel Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="PortKiller" FontSize="15" FontWeight="SemiBold" Foreground="#E0E0E0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Sidebar Links -->
                    <Border Grid.Row="1" BorderThickness="0,0,1,0" CornerRadius="0,0,0,16" x:Name="SidebarBorder">
                        <Border.Background>
                            <SolidColorBrush Color="#20000000" Opacity="0.5"/>
                        </Border.Background>
                        <Border.BorderBrush>
                            <SolidColorBrush Color="#40FFFFFF" Opacity="0.15"/>
                        </Border.BorderBrush>
                        <ScrollViewer VerticalScrollBarVisibility="Hidden" Margin="12">
                            <StackPanel>
                                <!-- Categories Section -->
                                <TextBlock Text="分类" FontSize="11" FontWeight="Bold" Foreground="#707070" Margin="12,12,0,8"/>
                                <Button Content="📋  所有端口" Click="SidebarButton_Click" Tag="AllPorts" Style="{StaticResource SidebarButton}"/>
                                <Button Content="⭐  收藏" Click="SidebarButton_Click" Tag="Favorites" Style="{StaticResource SidebarButton}"/>
                                <Button Content="👁  监控" Click="SidebarButton_Click" Tag="Watched" Style="{StaticResource SidebarButton}"/>

                                <!-- Process Types Section -->
                                <TextBlock Text="进程类型" FontSize="11" FontWeight="Bold" Foreground="#707070" Margin="12,24,0,8"/>
                                <Button Content="🌐  Web 服务器" Click="SidebarButton_Click" Tag="WebServer" Style="{StaticResource SidebarButton}"/>
                                <Button Content="💾  数据库" Click="SidebarButton_Click" Tag="Database" Style="{StaticResource SidebarButton}"/>
                                <Button Content="⚙  开发工具" Click="SidebarButton_Click" Tag="Development" Style="{StaticResource SidebarButton}"/>
                                <Button Content="🖥  系统" Click="SidebarButton_Click" Tag="System" Style="{StaticResource SidebarButton}"/>
                                <Button Content="📦  其他" Click="SidebarButton_Click" Tag="Other" Style="{StaticResource SidebarButton}"/>

                                <!-- Cloudflare Tunnels Section -->
                                <TextBlock Text="服务" FontSize="11" FontWeight="Bold" Foreground="#707070" Margin="12,24,0,8"/>
                                <Button Click="SidebarButton_Click" Tag="CloudflareTunnels" Style="{StaticResource SidebarButton}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="☁" Foreground="#F4AB3A" Margin="0,0,6,0"/>
                                        <TextBlock Text="Cloudflare 隧道"/>
                                    </StackPanel>
                                </Button>

                                <!-- Settings Section -->
                                <TextBlock Text="设置" FontSize="11" FontWeight="Bold" Foreground="#707070" Margin="12,24,0,8"/>
                                <Button Content="⚙  设置" Click="SidebarButton_Click" Tag="Settings" Style="{StaticResource SidebarButton}"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <!-- Right Content Area (Solid) -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Top Bar with Search and Controls -->
                    <Border Grid.Row="0" CornerRadius="0,16,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Background="#2A2A2A" Height="50">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Search and Actions - Centered (No Icon) -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Border BorderThickness="0" CornerRadius="20" Padding="0" Background="#1E1E1E">
                                    <Grid Width="400" Height="36">
                                        <TextBox x:Name="SearchBox" 
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 Padding="16,8,16,8"
                                                 FontSize="13"
                                                 Foreground="#E0E0E0"
                                                 CaretBrush="#E0E0E0"
                                                 VerticalContentAlignment="Center"
                                                 TextChanged="SearchBox_TextChanged"/>
                                        <TextBlock Text="搜索端口、进程..." 
                                                   FontSize="13" 
                                                   Foreground="#606060" 
                                                   VerticalAlignment="Center" 
                                                   Margin="16,0,0,0" 
                                                   IsHitTestVisible="False"
                                                   Visibility="{Binding Text.IsEmpty, ElementName=SearchBox, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </Grid>
                                </Border>
                            </StackPanel>

                            <!-- Window Controls -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,10,0">
                                <Button Click="RefreshButton_Click" Style="{StaticResource ModernButton}" Width="40" Height="40" ToolTip="Refresh (Ctrl+R)" Margin="0,0,8,0">
                                    <TextBlock Text="↻" FontSize="16" FontWeight="Bold" Margin="0,2,0,0"/>
                                </Button>
                                <Button Click="MinimizeButton_Click" Style="{StaticResource ModernButton}" Width="40" Height="40">
                                     <TextBlock Text="—" FontSize="12" FontWeight="Bold" Margin="0,4,0,0"/>
                                </Button>
                                <Button Click="MaximizeButton_Click" Style="{StaticResource ModernButton}" Width="40" Height="40">
                                     <Rectangle Width="10" Height="10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5"/>
                                </Button>
                                <Button Click="CloseButton_Click" Style="{StaticResource ModernButton}" Width="40" Height="40" Margin="8,0,0,0">
                                    <TextBlock Text="✕" FontSize="14"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Main Content Grid -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="320"/>
                        </Grid.ColumnDefinitions>

                        <!-- Port List with Solid Grey Background -->
                        <Grid Grid.Column="0" x:Name="PortsPanel" Background="#1E1E1E">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Solid Grey Header -->
                            <Border Grid.Row="0" BorderThickness="0,0,0,1" Padding="24,16" Background="#252525" BorderBrush="#353535">
                                <TextBlock x:Name="HeaderText" Text="所有端口" FontSize="20" FontWeight="SemiBold" Foreground="#E0E0E0"/>
                            </Border>

                            <!-- Ports ListView -->
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Padding="16">
                                <ItemsControl x:Name="PortsListView">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource Card}" Margin="0,0,0,12" MouseLeftButtonDown="PortItem_Click" Tag="{Binding}" Cursor="Hand">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="80"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Port Number with Status (Minimal Design) -->
                                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding DisplayPort}" FontSize="20" FontWeight="Bold" Foreground="#E0E0E0" HorizontalAlignment="Center"/>
                                                        <Ellipse Width="6" Height="6" Fill="#2ecc71" HorizontalAlignment="Center" Margin="0,4,0,0" 
                                                                 Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    </StackPanel>

                                                    <!-- Process Info -->
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                                                        <TextBlock Text="{Binding ProcessName}" FontSize="15" FontWeight="SemiBold" Foreground="#E0E0E0"/>
                                                        <TextBlock FontSize="12" Foreground="#A0A0A0" Margin="0,4,0,0">
                                                            <Run Text="地址:"/>
                                                            <Run Text="{Binding Address}" FontWeight="SemiBold"/>
                                                        </TextBlock>
                                                        <TextBlock FontSize="11" Foreground="#808080" Margin="0,2,0,0">
                                                            <Run Text="PID:"/>
                                                            <Run Text="{Binding Pid}"/>
                                                            <Run Text=" • 用户:"/>
                                                            <Run Text="{Binding User}"/>
                                                        </TextBlock>
                                                    </StackPanel>

                                                    <!-- Actions (Minimal Kill Button) -->
                                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Button Content="✕" 
                                                                Click="KillButton_Click" 
                                                                Tag="{Binding}"
                                                                Padding="10,6"
                                                                FontSize="16"
                                                                FontWeight="Normal"
                                                                Cursor="Hand"
                                                                ToolTip="终止进程">
                                                            <Button.Style>
                                                                <Style TargetType="Button">
                                                                    <Setter Property="Background" Value="Transparent"/>
                                                                    <Setter Property="Foreground" Value="#808080"/>
                                                                    <Setter Property="BorderThickness" Value="0"/>
                                                                    <Setter Property="Template">
                                                                        <Setter.Value>
                                                                            <ControlTemplate TargetType="Button">
                                                                                <Border Background="{TemplateBinding Background}" 
                                                                                        CornerRadius="6"
                                                                                        Padding="{TemplateBinding Padding}">
                                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                                </Border>
                                                                            </ControlTemplate>
                                                                        </Setter.Value>
                                                                    </Setter>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background">
                                                                                <Setter.Value>
                                                                                    <SolidColorBrush Color="#40FFFFFF" Opacity="0.1"/>
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                            <Setter Property="Foreground" Value="#e74c3c"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- Empty State -->
                            <StackPanel Grid.Row="1" x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                                <TextBlock Text="📊" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,16" Opacity="0.2"/>
                                <TextBlock Text="未找到端口" FontSize="18" Foreground="#A0A0A0" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Text="启动一些服务以在此处查看" FontSize="13" Foreground="#808080" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                            </StackPanel>

                            <!-- Loading State -->
                            <StackPanel Grid.Row="1" x:Name="LoadingState" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                                <TextBlock Text="📊" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,16" Opacity="0.2"/>
                                <TextBlock Text="正在加载..." FontSize="18" Foreground="#A0A0A0" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
                                    <Grid Width="20" Height="20" Margin="0,0,8,0">
                                        <Ellipse Stroke="#f39c12" 
                                                 StrokeThickness="2"
                                                 StrokeDashArray="12,6"
                                                 RenderTransformOrigin="0.5,0.5">
                                            <Ellipse.RenderTransform>
                                                <RotateTransform x:Name="LoadingSpinner" Angle="0"/>
                                            </Ellipse.RenderTransform>
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Visibility, ElementName=LoadingState}" Value="Visible">
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard x:Name="SpinAnimation">
                                                                    <Storyboard RepeatBehavior="Forever">
                                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                                                       From="0" To="360" Duration="0:0:1"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <DataTrigger.ExitActions>
                                                                <StopStoryboard BeginStoryboardName="SpinAnimation"/>
                                                            </DataTrigger.ExitActions>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                    </Grid>
                                    <TextBlock Text="正在扫描端口" FontSize="13" Foreground="#808080"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Status Bar - Solid Grey -->
                            <Border Grid.Row="2" BorderThickness="0,1,0,0" Padding="24,10" Background="#252525" BorderBrush="#353535">
                                <Grid>
                                    <TextBlock x:Name="StatusText" Text="就绪" FontSize="11" Foreground="#808080" VerticalAlignment="Center"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Ellipse Width="6" Height="6" Fill="#2ecc71" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="自动刷新" FontSize="11" Foreground="#808080" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>

                        <!-- Detail Panel - Solid Grey -->
                        <Border Grid.Column="1" x:Name="DetailPanel" BorderThickness="1,0,0,0" Padding="24" Visibility="Collapsed" CornerRadius="0,0,16,0" Background="#1E1E1E" BorderBrush="#353535">
                            <ScrollViewer VerticalScrollBarVisibility="Hidden">
                                <StackPanel>
                                    <TextBlock Text="端口详情" FontSize="18" FontWeight="Bold" Foreground="#E0E0E0" Margin="0,0,0,20"/>

                                    <!-- Detail Items - Solid Grey -->
                                    <StackPanel Margin="0,0,0,16">
                                        <TextBlock Text="端口" FontSize="10" FontWeight="Bold" Foreground="#808080" Margin="0,0,0,4"/>
                                        <Border CornerRadius="8" Padding="12,8" Background="#2A2A2A">
                                            <TextBlock x:Name="DetailPort" Text="-" FontSize="14" Foreground="#E0E0E0" FontWeight="SemiBold"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,16">
                                        <TextBlock Text="进程" FontSize="10" FontWeight="Bold" Foreground="#808080" Margin="0,0,0,4"/>
                                        <Border CornerRadius="8" Padding="12,8" Background="#2A2A2A">
                                            <TextBlock x:Name="DetailProcess" Text="-" TextWrapping="Wrap" FontSize="14" Foreground="#E0E0E0"/>
                                        </Border>
                                    </StackPanel>

                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                            <TextBlock Text="PID" FontSize="10" FontWeight="Bold" Foreground="#808080" Margin="0,0,0,4"/>
                                            <Border CornerRadius="8" Padding="12,8" Background="#2A2A2A">
                                                <TextBlock x:Name="DetailPid" Text="-" FontSize="14" Foreground="#E0E0E0"/>
                                            </Border>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                            <TextBlock Text="用户" FontSize="10" FontWeight="Bold" Foreground="#808080" Margin="0,0,0,4"/>
                                            <Border CornerRadius="8" Padding="12,8" Background="#2A2A2A">
                                                <TextBlock x:Name="DetailUser" Text="-" FontSize="14" Foreground="#E0E0E0"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Margin="0,0,0,16">
                                        <TextBlock Text="地址" FontSize="10" FontWeight="Bold" Foreground="#808080" Margin="0,0,0,4"/>
                                        <Border CornerRadius="8" Padding="12,8" Background="#2A2A2A">
                                            <TextBlock x:Name="DetailAddress" Text="-" FontSize="14" Foreground="#E0E0E0"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Margin="0,0,0,20">
                                        <TextBlock Text="命令" FontSize="10" FontWeight="Bold" Foreground="#808080" Margin="0,0,0,4"/>
                                        <Border CornerRadius="8" Padding="12,8" Background="#2A2A2A">
                                            <TextBlock x:Name="DetailCommand" Text="-" TextWrapping="Wrap" FontSize="12" Foreground="#E0E0E0"/>
                                        </Border>
                                    </StackPanel>

                                    <!-- Action Buttons - Clean Minimal Outline Style -->
                                    <StackPanel Margin="0,10,0,0">
                                        <Button x:Name="ShareTunnelButton" 
                                                Click="ShareTunnelButton_Click"
                                                Style="{StaticResource OutlineButton}"
                                                Margin="0,0,0,10"
                                                HorizontalAlignment="Stretch">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="☁" Foreground="#F4AB3A" Margin="0,0,6,0"/>
                                                <TextBlock Text="通过隧道分享"/>
                                            </StackPanel>
                                        </Button>

                                        <Button x:Name="FavoriteButton" 
                                                Content="⭐  添加到收藏" 
                                                Click="FavoriteButton_Click"
                                                Style="{StaticResource OutlineButton}"
                                                Margin="0,0,0,10"
                                                HorizontalAlignment="Stretch"/>

                                        <Button x:Name="WatchButton" 
                                                Content="👁  监控端口" 
                                                Click="WatchButton_Click"
                                                Style="{StaticResource OutlineButton}"
                                                HorizontalAlignment="Stretch"/>
                                    </StackPanel>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>

                    <!-- Cloudflare Tunnels Panel (Hidden by Default) -->
                    <Grid Grid.Column="0" Grid.ColumnSpan="2" x:Name="TunnelsPanel" Background="#1E1E1E" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Tunnels Header -->
                        <Border Grid.Row="0" BorderThickness="0,0,0,1" Padding="24,16" Background="#252525" BorderBrush="#353535">
                            <Grid>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="☁" FontSize="20" Margin="0,0,10,0" Foreground="#F4AB3A"/>
                                    <TextBlock Text="Cloudflare 隧道" FontSize="20" FontWeight="SemiBold" Foreground="#E0E0E0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="StopAllTunnelsButton" 
                                            Content="停止全部" 
                                            Click="StopAllTunnels_Click"
                                            Visibility="Collapsed"
                                            Margin="0,0,8,0">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="#e74c3c"/>
                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Padding" Value="12,6"/>
                                                <Setter Property="FontSize" Value="12"/>
                                                <Setter Property="Cursor" Value="Hand"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#c0392b"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <Button Content="↻ 刷新" Click="RefreshTunnels_Click" Style="{StaticResource ModernButton}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Cloudflared Not Installed Warning -->
                        <Border Grid.Row="1" 
                                x:Name="CloudflaredWarning"
                                Background="#3d2918" 
                                BorderBrush="#8B4513" 
                                BorderThickness="1" 
                                CornerRadius="8" 
                                Padding="16"
                                Margin="24,16"
                                VerticalAlignment="Top"
                                Visibility="Collapsed">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="⚠" FontSize="20" Margin="0,0,12,0" Foreground="#f39c12"/>
                                    <TextBlock Text="未安装 cloudflared" FontSize="16" FontWeight="Bold" Foreground="#f39c12" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" Foreground="#E0E0E0" Margin="32,0,0,8">
                                    要使用 Cloudflare 隧道，您需要安装 cloudflared：
                                </TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#B0B0B0" Margin="32,0,0,0" FontFamily="Consolas" FontSize="11">
                                    • 从以下地址下载：https://github.com/cloudflare/cloudflared/releases
                                    • 或使用 Chocolatey：choco install cloudflared
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- Tunnels List -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Padding="16">
                            <ItemsControl x:Name="TunnelsListView">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource Card}" Margin="0,0,0,12" Cursor="Hand">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Status Indicator -->
                                                <StackPanel Grid.Column="0" Margin="0,0,16,0" VerticalAlignment="Center">
                                                    <Ellipse Width="12" Height="12" 
                                                             Fill="{Binding StatusColor}"
                                                             HorizontalAlignment="Center"
                                                             Margin="0,0,0,4"/>
                                                    <TextBlock Text="{Binding StatusText}" 
                                                               FontSize="10" 
                                                               Foreground="#A0A0A0"
                                                               HorizontalAlignment="Center"/>
                                                </StackPanel>

                                                <!-- Port and URL Info -->
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock FontSize="18" FontWeight="Bold" Foreground="#E0E0E0" Margin="0,0,0,4">
                                                        <Run Text="端口"/>
                                                        <Run Text="{Binding Port}" Foreground="#3498db"/>
                                                    </TextBlock>
                                                    <TextBlock Text="{Binding DisplayUrl}" 
                                                               FontSize="13" 
                                                               Foreground="#2ecc71"
                                                               FontFamily="Consolas"
                                                               Margin="0,2,0,0"/>
                                                    <TextBlock Text="{Binding LastError}" 
                                                               FontSize="11" 
                                                               Foreground="#e74c3c"
                                                               TextWrapping="Wrap"
                                                               Margin="0,4,0,0"/>
                                                </StackPanel>

                                                <!-- Actions -->
                                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <!-- Uptime -->
                                                    <Border Background="#3A3A3A" 
                                                            CornerRadius="6" 
                                                            Padding="10,6" 
                                                            Margin="0,0,8,0"
                                                            VerticalAlignment="Center"
                                                            Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                            <TextBlock Text="⏱" FontSize="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{Binding Uptime}" FontSize="14" Foreground="#E0E0E0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </StackPanel>
                                                    </Border>

                                                    <!-- Copy Button -->
                                                    <Button ToolTip="复制 URL"
                                                            Click="TunnelCopy_Click"
                                                            Tag="{Binding}"
                                                            Style="{StaticResource ModernButton}"
                                                            Width="36" Height="36"
                                                            Padding="6"
                                                            Margin="0,0,4,0">
                                                        <TextBlock Text="📋" FontSize="16"/>
                                                    </Button>

                                                    <!-- Open in Browser Button -->
                                                    <Button ToolTip="在浏览器中打开"
                                                            Click="TunnelOpen_Click"
                                                            Tag="{Binding}"
                                                            Style="{StaticResource ModernButton}"
                                                            Width="36" Height="36"
                                                            Padding="6"
                                                            Margin="0,0,4,0">
                                                        <TextBlock Text="🌐" FontSize="16"/>
                                                    </Button>

                                                    <!-- Restart Button -->
                                                    <Button ToolTip="重新生成"
                                                            Click="TunnelRestart_Click"
                                                            Tag="{Binding}"
                                                            Style="{StaticResource ModernButton}"
                                                            Width="36" Height="36"
                                                            Padding="6"
                                                            Background="#f39c12"
                                                            Margin="0,0,4,0">
                                                        <TextBlock Text="🔄" FontSize="16"/>
                                                    </Button>

                                                    <!-- Stop Button -->
                                                    <Button ToolTip="停止隧道"
                                                            Click="TunnelStop_Click"
                                                            Tag="{Binding}"
                                                            Width="36" Height="36"
                                                            Padding="6">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Background" Value="#e74c3c"/>
                                                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                                                <Setter Property="BorderThickness" Value="0"/>
                                                                <Setter Property="Cursor" Value="Hand"/>
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="6">
                                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                            </Border>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#c0392b"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                        <TextBlock Text="✕" FontSize="14"/>
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <!-- Tunnels Loading State -->
                        <StackPanel Grid.Row="1" x:Name="TunnelsLoadingState" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                            <TextBlock Text="☁" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,16" Opacity="0.5" Foreground="#F4AB3A">
                                <TextBlock.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                               From="0.3" To="0.7" Duration="0:0:1.5"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </TextBlock.Triggers>
                            </TextBlock>
                            <TextBlock x:Name="TunnelsLoadingText" Text="正在加载..." FontSize="20" Foreground="#E0E0E0" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
                                <Ellipse Width="24" Height="24" 
                                         Stroke="#3498db" 
                                         StrokeThickness="3"
                                         StrokeDashArray="18,9"
                                         Margin="0,0,12,0">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform x:Name="TunnelsLoadingSpinner" CenterX="12" CenterY="12"/>
                                    </Ellipse.RenderTransform>
                                    <Ellipse.Triggers>
                                        <EventTrigger RoutedEvent="Ellipse.Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetName="TunnelsLoadingSpinner"
                                                                   Storyboard.TargetProperty="Angle"
                                                                   From="0" To="360" Duration="0:0:0.8"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Ellipse.Triggers>
                                </Ellipse>
                                <TextBlock x:Name="TunnelsLoadingSubText" Text="正在检查隧道状态" FontSize="14" Foreground="#A0A0A0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Tunnels Empty State -->
                        <StackPanel Grid.Row="1" x:Name="TunnelsEmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed">
                            <TextBlock Text="☁" FontSize="64" HorizontalAlignment="Center" Margin="0,0,0,16" Opacity="0.3" Foreground="#F4AB3A"/>
                            <TextBlock Text="无活动隧道" FontSize="20" Foreground="#A0A0A0" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBlock Text="从端口列表启动隧道以创建公共 URL" 
                                       FontSize="13" 
                                       Foreground="#808080" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,8,0,16"
                                       TextAlignment="Center"/>
                            <TextBlock TextWrapping="Wrap" 
                                       FontSize="12" 
                                       Foreground="#606060" 
                                       HorizontalAlignment="Center" 
                                       TextAlignment="Center"
                                       MaxWidth="400">
                                💡 提示：Cloudflare 隧道通过自动 HTTPS 使您的本地主机端口可从互联网的任何位置访问
                            </TextBlock>
                        </StackPanel>

                        <!-- Tunnels Status Bar -->
                        <Border Grid.Row="2" BorderThickness="0,1,0,0" Padding="24,10" Background="#252525" BorderBrush="#353535" CornerRadius="0,0,16,0">
                            <Grid>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="TunnelStatusDot" Width="6" Height="6" Fill="#808080" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="TunnelStatusText" Text="0 个活动隧道" FontSize="11" Foreground="#808080" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <!-- Cloudflared Installation Status -->
                                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                                        <Ellipse x:Name="CloudflaredStatusDot" Width="6" Height="6" Fill="#e74c3c" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock x:Name="CloudflaredStatusText" Text="未安装 cloudflared" FontSize="11" Foreground="#808080" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <!-- Auto-refresh Indicator -->
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="6" Height="6" Fill="#2ecc71" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="自动刷新" FontSize="11" Foreground="#808080" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
