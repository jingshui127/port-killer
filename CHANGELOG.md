# PortManager 更新日志

## v3.0.0 (2026-02-22)

### 重大更新

#### 1. 桌面版本发布
- **WinForms Blazor Hybrid**：全新桌面应用程序
- **统一代码库**：桌面版和 Web 版共用同一套代码
- **离线运行**：无需浏览器，独立运行
- **系统集成**：更好的 Windows 系统集成

#### 2. 项目结构重构
- **共享类库**：提取 Core 和 Shared 类库
- **多平台支持**：Web (Blazor Server) + Desktop (WinForms)
- **代码复用**：避免重复代码，易于维护

### 项目结构
```
port-killer/
├── platforms/
│   ├── blazor/PortManager/      # Web 版本
│   └── desktop/PortManager.Desktop/  # 桌面版本
├── src/
│   ├── PortManager.Core/        # 核心类库 (Models + Services)
│   └── PortManager.Shared/      # 共享组件 (Razor)
```

## v2.1.0 (2026-02-22)

### 新增功能

#### 1. 防火墙规则检测
- **入站/出站状态显示**：自动检测端口防火墙规则状态
- **智能规则解析**：支持中英文 netsh 输出解析
- **状态图标**：使用箭头图标直观显示访问方向
- **端口 22 等特殊端口**：正确识别 SSH 等服务的双向访问

#### 2. 链接健康检测
- **自动检测**：刷新隧道时自动检测链接状态
- **手动检测**：支持手动重新检测链接
- **状态显示**：
  - 🟢 链接正常
  - 🔴 无法访问（显示 HTTP 状态码）
  - 🟡 检测失败
- **检测历史**：显示上次检测时间

#### 3. 进程详情增强
- **CPU 使用率监控**：实时显示进程 CPU 占用
- **内存使用显示**：自动切换 KB/MB 单位
- **进程优先级**：显示进程优先级（实时/高/正常/低等）
- **启动时间**：显示相对启动时间（X 分钟/小时/天前）
- **子进程数量**：显示子进程计数
- **状态指示器**：运行中/未响应/已退出状态

#### 4. 性能优化
- **刷新速度提升**：从 22 秒优化到 2 秒
- **批量查询**：使用 tasklist 批量获取进程信息
- **减少 WMI 查询**：缓存进程信息避免重复查询
- **异步执行**：操作按钮异步执行避免阻塞 UI

#### 5. UI 优化
- **紧凑布局**：进程详情弹窗更紧凑
- **按钮配色**：渐变色彩提升视觉效果
- **状态指示器**：彩色圆点显示端口状态
- **系统进程标识**：🛡️ 标识受保护的系统进程
- **导出功能**：支持 JSON 和 CSV 格式导出

### 修复问题

1. **编码问题**：修复 netsh 中文输出乱码问题
2. **空引用警告**：修复多处可能的空引用
3. **异步警告**：修复 CS4014 异步调用警告
4. **日志方法**：修复 XTrace.Log.Warning 不存在的问题
5. **URL 保存**：修复隧道 URL 未正确保存到 JSON 的问题

### 代码清理

- 删除未使用的示例文件（FetchData.razor, SurveyPrompt.razor 等）
- 删除 WeatherForecast 相关代码
- 优化代码结构，添加详细注释

---

## v2.0-20260221 (2026-02-21)

### 新增功能

#### 1. 智能刷新机制
- **自动刷新功能**：支持自动刷新端口列表，默认间隔 15 秒
- **智能防重复刷新**：添加最小刷新间隔（2秒），避免重复刷新
- **手动刷新优先**：手动刷新可中断自动刷新，等待当前刷新完成后执行
- **实时刷新进度**：刷新过程中实时显示已耗时，带脉冲动画效果

#### 2. 状态信息展示
- **最后刷新时间**：显示上次刷新的具体时间戳（HH:mm:ss 格式）
- **刷新耗时统计**：显示上次刷新所用的总时间
- **刷新计数器**：记录并显示总刷新次数，刷新时图标旋转
- **状态信息布局**：状态信息单独一行居中显示，清晰美观

#### 3. 通知系统增强
- **通知历史弹窗**：可查看所有历史通知记录
- **端口启动通知**：端口重新打开时显示通知
- **端口终止通知**：端口关闭时显示通知
- **通知持久化**：所有通知记录到通知历史中

#### 4. 性能优化
- **增量更新**：端口刷新采用增量更新，避免界面闪烁
- **缓存机制**：启动时优先从缓存加载端口数据
- **并发控制**：使用 SemaphoreSlim 防止并发刷新冲突
- **页面生命周期管理**：添加 Dispose 保护，防止页面释放后操作

#### 5. 界面优化
- **布局重构**：操作按钮和状态信息分组显示，更清晰
- **实时反馈**：刷新过程中按钮显示"刷新中..."状态
- **动画效果**：添加旋转、脉冲等动画提升交互体验
- **响应式设计**：支持不同屏幕尺寸的适配

### 修复问题

1. **ObjectDisposedException**：修复页面释放后定时器仍尝试访问的问题
2. **刷新冲突**：修复手动刷新和自动刷新冲突的问题
3. **空引用警告**：修复多处可能的空引用警告
4. **端口计数不一致**：统一端口统计逻辑，以唯一端口数为基准

### 技术改进

- 添加 `_isDisposed` 标志防止页面释放后操作
- 优化 `SemaphoreSlim` 使用，确保正确释放
- 改进定时器管理，避免内存泄漏
- 统一刷新计数逻辑，自动和手动刷新都计入总数

---

## v1.0.0 (初始版本)

### 基础功能
- 端口扫描和管理
- 进程终止功能
- 隧道管理（Cloudflared）
- 主题切换（深色/浅色）
- 响应式布局
